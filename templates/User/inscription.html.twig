{% extends "layout.html.twig" %}

{% block content %}
    {# Form Membre #}
    <div class="container">
        <div id="editor" class="row">
            <div class="col-xs-8 col-xs-push-2">
                <h3>Inscrivez-vous en tant que membre</h3>
                {{ form(formMembre, {attr:{novalidate: 'novalidate' }}) }}
            </div>
        </div>


        {# Form Organisateur #}

        <div id="editor" class="row">
            <div class="col-xs-8 col-xs-push-2">
                <h3>Inscrivez-vous en tant qu'organisateur</h3>
                {{ form(formOrga, {attr:{novalidate: 'novalidate' }}) }}
            </div>
        </div>
    </div>
{% endblock %}

